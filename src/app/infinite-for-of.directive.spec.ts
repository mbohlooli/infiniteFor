import { InfiniteForOfDirective } from './infinite-for-of.directive';

describe('InfiniteForOfDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiniteForOfDirective();
    expect(directive).toBeTruthy();
  });
});
