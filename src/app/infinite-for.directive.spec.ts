import { InfiniteForDirective } from './infinite-for.directive';

describe('InfiniteForDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiniteForDirective();
    expect(directive).toBeTruthy();
  });
});
